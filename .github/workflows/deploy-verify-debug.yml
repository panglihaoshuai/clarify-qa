# TEST: brand new minimal workflow
name: Deploy Verify Debug

on:
  workflow_dispatch:
  push:

jobs:
  verify:
    name: Post-Deploy Verification
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
